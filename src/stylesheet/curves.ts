export const curves: Curves = {
  xs: '2px',
  sm: '6px',
  checkbox: '3px',
  lg: '40px',
  circle: '100000px',
};

export type Curve = 'xs' | 'sm' | 'checkbox' | 'circle' | 'lg';

export type Curves = {
  xs: string;
  sm: string;
  checkbox: string;
  circle: string;
  lg: string;
};
