export type Config = {
  routes: {
    todoList: 'TodoList';
    listItem: 'ListItem';
  };
};

export const config: Config = {
  routes: {
    todoList: 'TodoList',
    listItem: 'ListItem',
  },
};
